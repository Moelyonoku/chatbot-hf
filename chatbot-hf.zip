PK  jZ            	   chatbot-hf/UT	 ddux
     PK  jZ            4   chatbot-hf/package.jsonUT	 ddux
     { "name": "chatbot-hf", "version": "1.0.0", "scripts": { "start": "vercel dev" } }PK  jZ               chote-lf/api/UT	 ddux
     PK  jZ            (   chatbot-hf/api/chat.jsUT	 ddux
     export default async function handler(req, res) {
  if (req.method !== "POST") {
    return res.status(405).json({ error: "Only POST method allowed" });
  }

  try {
    const { message } = req.body;

    // Model AI From Tuggcing Face
    const response = await fetch(
      "https://api-inference.huggingface.co/models/facebook/blenderbot-400M-distill",
      {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${process.env.HF_API_KEY}`,
        "Content-Type": "application/json"
      },
      body: JSON.stringify({ inputs: message })
    }
    );

    const data = await response.json();
    const reply = data[0]?.generated_text || "€ Maaf, server sibuk.";

    res.status(200).json({ reply });
  } catch (err) {
    console.error(err);
    res.status(500).json( { error: "Server error" });
  }
}
PK4  jZ            	               chatbot-hf/UT dux
     PK4  jZ             4             chatbot-hf/package.jsonUT dux
     PK4  jZ                       F  chatbot-hf/api/UT dux
     PK
 
 jZ            
             chatbot-hf/api/chat.jsUT dux
     PK      F @ /    